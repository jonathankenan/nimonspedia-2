# AAAAAAAA


# Kalo mo run,
```bash
docker compose up --build -d
```
trus buka
```bash
http://localhost:8080
```

# Klo mo ngequery sql bisa langsung
```bash
docker exec -it nimonspedia_db mysql -u user -p -D nimonspedia -e "SELECT * FROM users;"
```
password : password

## btw ini terlanjur pake apache (struktur dari template), jadinya struktur folder ya begini :v

