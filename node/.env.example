# Node.js Environment Variables
# Copy this file to node/.env and modify as needed

# Environment
NODE_ENV=development

# Server Ports
PORT=3000
WS_PORT=3001

# Database Configuration (Shared with PHP)
DB_HOST=db
DB_USER=user
DB_PASSWORD=password
DB_NAME=nimonspedia
DB_PORT=3306

# Redis Configuration (Session Sharing)
REDIS_HOST=redis
REDIS_PORT=6379

# JWT Secret (⚠️ CHANGE IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-in-production-please

# CORS Settings (Optional)
# CORS_ORIGIN=http://localhost:3000

# Logging
# LOG_LEVEL=debug
